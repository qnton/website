---
import Typography from "@/components/ui/typography.astro";

interface Props {
  page: string;
}

const { page } = Astro.props;

const year = new Date().getFullYear();
---

<footer>
  <nav class="animate-fade animate-once flex justify-between md:mt-32">
    <ul class="float-left mb-7 ml-5 flex gap-6 text-3xl">
      <Typography
        className="text-3xl opacity-60"
        component="li"
        variant="p"
      >
        © {year}
      </Typography>
    </ul>
    <ul class="float-right mr-5 mb-7 flex gap-6">
      <li>
        <Typography
          className="cursor-pointer text-3xl opacity-60 transition-opacity duration-300 hover:opacity-100"
          component="a"
          href={page !== "legal-notice" ? "/legal-notice" : "#"}
          variant="p"
        >
          Legal notice
        </Typography>
      </li>
    </ul>
  </nav>
</footer>
